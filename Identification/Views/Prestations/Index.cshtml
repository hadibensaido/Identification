@model IEnumerable<MetierIdentification.Models.Prestations>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.datePrestation)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.datePrestation)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idPrestation }) |
            @Html.ActionLink("Details", "Details", new { id=item.idPrestation }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idPrestation })
        </td>
    </tr>
}

</table>
