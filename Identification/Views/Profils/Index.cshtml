@model IEnumerable<MetierIdentification.Models.Profil>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.libelleProfil)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.libelleProfil)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idProfil }) |
            @Html.ActionLink("Details", "Details", new { id=item.idProfil }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idProfil })
        </td>
    </tr>
}

</table>
