@model IEnumerable<MetierIdentification.Models.Client>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Invitation.libelleInvitation)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Segment.libelleSegment)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.nomClient)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Invitation.libelleInvitation)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Segment.libelleSegment)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.nomClient)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idClient }) |
            @Html.ActionLink("Details", "Details", new { id=item.idClient }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idClient })
        </td>
    </tr>
}

</table>
